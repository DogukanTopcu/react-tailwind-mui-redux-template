import { Route, Routes } from "react-router-dom";

function App() {
  return (
    <div className="App">
      <Routes>
        {/* <Route index element={App} /> */}
      </Routes>
      react + router
    </div>
  );
}

export default App;
